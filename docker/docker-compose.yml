services:
  5dcol-backend:
    image: ${FIVE_DCOL_BACKEND_IMAGE:-ghcr.io/forkkillet/5dcol-backend:latest}
    restart: unless-stopped
    environment:
      - PORT=5161
    volumes:
      - ./data:/app/data
